# HPGe detectors

A germanium detector gives two handles on an interaction: how much of the
released charge it collects, and the shape of the current pulse it produces.
Both are computed from the same steps, but they answer different questions, so
they are documented separately.

```{toctree}
:maxdepth: 2

charge
psd
```
